"""سازگاری با نسخه‌های قدیمی pip/setuptools.

تنظیمات اصلی در pyproject.toml است؛ این فایل فقط اجازه می‌دهد نصب editable
(pip install -e .) روی pipهای قدیمی‌تر هم کار کند.
"""

from setuptools import setup

setup()
